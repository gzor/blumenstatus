name: CI

on: [push]

jobs:
  build:

    runs-on: self-hosted
    # needs jq and platformio installed installed

    steps:
    - uses: actions/checkout@v2
    - name:  Check Env Var 
      run: echo $PLATFORMIO_BUILD_FLAGS > /home/osmc/pio_build_flags.txt
      env: 
        PLATFORMIO_BUILD_FLAGS: ${{ secrets.pio_build_flag }}
      
    - name: Run PlattformIO
      run: |
        sed -i 's/REPLACETHIS/$PLATFORMIO_BUILD_FLAGS/g' platformio.ini
        pio run -t upload
      
    - name: cleanup old action artifacts
      run: |
        chmod u+x workflow-scripts/delete-old-artifacts.sh
        workflow-scripts/delete-old-artifacts.sh ${{ secrets.REPO_ACCESS_TOKEN }}
      
#    - name: Upload Compiled Binary as Artifact
#      uses: actions/upload-artifact@v1.0.0
#      with:
#        name: firmware.bin 
#        path: .pio/build/esp32doit-devkit-v1/firmware.bin
        
      
#    - name: Run a multi-line script
#      run: |
#        echo Add other actions to build,
#        echo test, and deploy your project.
